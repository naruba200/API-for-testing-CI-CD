pipeline {
    agent any 
    stages {
        stage ('clone') {
            steps 
                {
                    git branch: 'main', url: 'https://github.com/naruba200/API-for-testing-CI-CD.git'
                }
        }
    }
}
